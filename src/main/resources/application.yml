logging:
  level:
    org.example: DEBUG

spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database: mysql
    hibernate:
      ddl-auto: create
  datasource:
    url: "jdbc:mysql://localhost:3306/hrm"
    username: "root"
    password: "root"

  mvc:
    converters:
      preferred-json-mapper: jackson
